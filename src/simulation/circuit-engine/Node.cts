import { CircuitNode } from './types.cts';

// For now, the CircuitNode interface in types.ts is sufficient.
// If Nodes need more complex behavior or state later (e.g., storing
// connected components for graph traversal), this file can be expanded
// into a full class.

// Export the type for consistency if needed elsewhere
export type { CircuitNode }; 